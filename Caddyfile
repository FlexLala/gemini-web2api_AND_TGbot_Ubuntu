# Вариант A: Если 3X-UI терминирует TLS и шлёт plain HTTP на 8443
:8443 {
    bind 127.0.0.1
    reverse_proxy localhost:3000
}

# Вариант B: Если 3X-UI проксирует TLS (SNI) без termination на 8443
# :8443 {
#     bind 127.0.0.1
#     tls internal
#     reverse_proxy localhost:3000
# }
